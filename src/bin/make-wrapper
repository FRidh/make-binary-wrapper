#!/usr/bin/env python3

# Entry point is created manually to avoid dependencies.

import sys
sitepackages = "@sitePackages@"
sys.path.insert(0, sitepackages)
import libwrapper.make_wrapper
libwrapper.make_wrapper.main()
